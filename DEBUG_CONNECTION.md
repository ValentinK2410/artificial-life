# 🔍 Диагностика подключения к серверу

## Текущий статус:
- ✅ Сервер запущен (PID: 1399092)
- ✅ Порт 3000 слушается на всех интерфейсах (:::3000)

## Проверка подключения:

### 1. Проверка локального подключения:
```bash
curl http://localhost:3000
```
Должен вернуть JSON: `{"status":"ok","message":"Artificial Life Server is running",...}`

### 2. Проверка через домен:
```bash
curl http://game.dekan.pro
curl https://game.dekan.pro
```

### 3. Проверка WebSocket через Nginx:
```bash
# Проверка, что Nginx проксирует /socket.io/
curl -i -N -H "Connection: Upgrade" -H "Upgrade: websocket" \
  -H "Sec-WebSocket-Version: 13" -H "Sec-WebSocket-Key: test" \
  http://game.dekan.pro/socket.io/?EIO=4&transport=polling
```

### 4. Проверка логов Nginx:
```bash
tail -f /var/www/httpd-logs/game.dekan.pro.error.log
tail -f /var/www/httpd-logs/game.dekan.pro.access.log
```

### 5. Проверка логов Node.js:
Если сервер запущен через ISPmanager, логи можно посмотреть в панели управления.

## Возможные проблемы:

### Проблема 1: Nginx не проксирует WebSocket
**Решение:** Убедитесь, что в конфигурации Nginx есть блок:
```nginx
location /socket.io/ {
    proxy_pass http://unix:/var/www/www-root/data/nodejs/0.sock;
    # или proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    ...
}
```

### Проблема 2: Сервер слушает только на IPv6
**Решение:** Сервер уже слушает на всех интерфейсах (:::3000), это нормально.

### Проблема 3: CORS блокирует подключение
**Решение:** Проверьте настройки CORS в `backend/server.js` - они должны разрешать запросы с `game.dekan.pro`.

### Проблема 4: Unix socket не настроен
**Решение:** Если используется Unix socket, убедитесь что:
- Socket файл существует: `/var/www/www-root/data/nodejs/0.sock`
- Права доступа правильные
- Nginx настроен на использование socket

## Быстрая проверка:

Выполните на сервере:
```bash
# 1. Проверка локального подключения
curl http://localhost:3000

# 2. Проверка через домен
curl http://game.dekan.pro

# 3. Проверка процесса
ps aux | grep "node.*server.js" | grep -v grep

# 4. Проверка порта
netstat -tuln | grep 3000
```

## После исправления:

1. Перезапустите Nginx: `sudo systemctl reload nginx` или через ISPmanager
2. Перезапустите Node.js приложение через ISPmanager
3. Обновите страницу игры в браузере
4. Откройте консоль браузера (F12) и проверьте ошибки подключения
