# Инструкция по настройке в ISPmanager

## Шаг 1: Настройка Node.js приложения

1. Войдите в ISPmanager
2. Перейдите: **WWW → Node.js приложения**
3. Нажмите **Создать** или найдите существующее приложение для `game.dekan.pro`
4. Заполните настройки:

   **Основные параметры:**
   - **Домен**: `game.dekan.pro`
   - **Путь к приложению**: `/var/www/www-root/data/www/game.dekan.pro/backend`
   - **Файл запуска**: `server.js`
   - **Порт**: `3000` (или другой свободный)
   - **Режим**: `Production`

   **Переменные окружения:**
   ```
   NODE_ENV=production
   PORT=3000
   ```

5. **Сохраните** и **запустите** приложение
6. Проверьте статус - должно быть "Запущено"

## Шаг 2: Настройка Nginx

1. В ISPmanager перейдите: **WWW → Домены → game.dekan.pro**
2. Откройте **Настройки**
3. Найдите раздел **Дополнительные директивы nginx**
4. **УДАЛИТЕ** всю существующую конфигурацию
5. **ВСТАВЬТЕ** полную конфигурацию из файла `NGINX_FULL_CONFIG.conf`

   Или скопируйте оба блока (HTTP и HTTPS) из файла `NGINX_FULL_CONFIG.conf`

6. **Сохраните** изменения
7. ISPmanager автоматически перезагрузит Nginx

## Шаг 3: Проверка

### Проверьте логи Nginx:

```bash
tail -f /var/www/httpd-logs/game.dekan.pro.error.log
```

### Проверьте работу:

1. Откройте `https://game.dekan.pro` в браузере
2. Должна открыться страница с формой входа
3. Откройте консоль браузера (F12)
4. Введите имя и нажмите "Подключиться"
5. Проверьте, что нет ошибок подключения

## Если все еще 502 Bad Gateway:

1. **Проверьте Node.js приложение**:
   - Убедитесь, что оно запущено в ISPmanager
   - Проверьте логи приложения в ISPmanager
   - Убедитесь, что путь правильный: `/var/www/www-root/data/www/game.dekan.pro/backend`

2. **Проверьте файлы на сервере**:
   ```bash
   ls -la /var/www/www-root/data/www/game.dekan.pro/index.html
   ls -la /var/www/www-root/data/www/game.dekan.pro/backend/server.js
   ```

3. **Проверьте Unix socket**:
   ```bash
   ls -la /var/www/www-root/data/nodejs/0.sock
   ```

4. **Перезапустите Node.js приложение** в ISPmanager

## Структура файлов на сервере должна быть:

```
/var/www/www-root/data/www/game.dekan.pro/
├── index.html
├── main.js
├── world.js
├── agents.js
├── network.js
├── style.css
├── package.json
└── backend/
    ├── server.js
    ├── package.json
    └── node_modules/
```

## Готово! 🎉

После выполнения всех шагов игра должна работать на `https://game.dekan.pro`
