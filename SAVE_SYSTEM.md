# 💾 Система сохранения прогресса

## Как работает сохранение

### Автоматическое сохранение

1. **При изменении состояния мира:**
   - Добавление/обновление агентов
   - Добавление ресурсов, животных, хищников
   - Постройка костров и структур
   - Изменение погоды/времени

2. **Периодическое автосохранение:**
   - Каждые **60 секунд** все миры сохраняются автоматически

3. **При отключении игрока:**
   - Мир сохраняется перед отключением
   - Агенты игрока **НЕ удаляются** - они остаются в мире для следующего входа

4. **При завершении сервера:**
   - Все миры сохраняются при получении сигналов SIGINT/SIGTERM

### Загрузка при старте

- При запуске сервера автоматически загружаются все сохраненные миры
- Если сохранение найдено, мир восстанавливается из файла
- Если сохранения нет, создается новый мир

## Где хранятся сохранения

**Путь:** `backend/saves/world_<worldId>.json`

**Пример:** `backend/saves/world_default.json`

## Что сохраняется

### Состояние мира:
- ✅ Агенты (все характеристики: здоровье, энергия, позиция, инвентарь, навыки, запасы)
- ✅ Ресурсы (тип, позиция, количество)
- ✅ Животные (тип, позиция, состояние)
- ✅ Хищники (тип, позиция, состояние)
- ✅ Костры (позиция, интенсивность, дрова)
- ✅ Постройки (тип, позиция, владелец)
- ✅ Террейн (деревья, камни, кусты)
- ✅ Время и погода (день, время суток, погода)

### Что НЕ сохраняется:
- ❌ Активные подключения игроков (socketId) - они переподключаются при входе
- ❌ Временные данные (например, анимации)

## Восстановление агентов при повторном входе

Когда игрок входит в игру:

1. Сервер загружает сохраненный мир
2. Находит агентов с `ownerId`, соответствующим игроку
3. Отправляет состояние мира клиенту
4. Клиент восстанавливает агентов из `worldState`

**Важно:** Агенты сохраняются с `ownerId`, но при переподключении `socket.id` меняется. Нужно сопоставлять по имени игрока или использовать постоянный ID игрока.

## Улучшения для будущего

1. **Постоянный ID игрока:**
   - Использовать UUID вместо socket.id для идентификации
   - Сохранять связь socket.id -> playerId

2. **База данных:**
   - Перейти с файлов на SQLite или MongoDB
   - Более надежное хранение и быстрый поиск

3. **Резервные копии:**
   - Автоматические бэкапы каждые N часов
   - Хранение нескольких версий сохранений

4. **Оптимизация:**
   - Сохранение только изменений (дельта)
   - Сжатие JSON файлов

## Ручное управление сохранениями

### Удаление сохранения мира:
```javascript
// В коде сервера
import { deleteWorldSave } from './saveManager.js';
await deleteWorldSave('default');
```

### Просмотр сохранений:
```bash
# На сервере
ls -lh backend/saves/
cat backend/saves/world_default.json
```

## Настройки

**Интервал автосохранения:** 60 секунд (можно изменить в `server.js`)

**Путь сохранений:** `backend/saves/` (можно изменить в `saveManager.js`)

## Формат файла сохранения

```json
{
  "id": "default",
  "agents": [
    {
      "id": "agent_123",
      "name": "Иван",
      "health": 85,
      "energy": 60,
      "position": { "x": 100, "y": 200 },
      "inventory": [...],
      "foodStorage": [...],
      "experience": {...},
      "ownerId": "socket_id_123"
    }
  ],
  "resources": [...],
  "animals": [...],
  "predators": [...],
  "fires": [...],
  "buildings": [...],
  "day": 5,
  "timeOfDay": "day",
  "weather": "sunny",
  "lastSaved": "2024-01-15T12:00:00.000Z"
}
```
