# Artificial Life - Сетевая симуляция жизни

Многопользовательская игра-симуляция, где игроки могут строить, взаимодействовать и развивать свой мир вместе.

## 🚀 Быстрый старт

### Установка и запуск сервера

1. Перейдите в папку `backend`:
```bash
cd backend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите сервер:
```bash
npm start
```

Сервер запустится на `http://localhost:3000`

**Или используйте скрипт:**
```bash
./start-server.sh
```

### ⚠️ Решение проблем

**Если видите ошибку "Не удалось подключиться к серверу":**

1. Убедитесь, что сервер запущен:
```bash
cd backend
npm start
```

2. Проверьте, что порт 3000 свободен:
```bash
lsof -i :3000
```

3. Если сервер не запускается, проверьте, установлены ли зависимости:
```bash
cd backend
npm install
```

4. **Офлайн режим**: Если не можете запустить сервер, нажмите кнопку "Играть офлайн" в окне подключения. Игра будет работать без сетевых функций.

### Запуск клиента

1. Откройте `index.html` в браузере или используйте локальный сервер:
```bash
# Используя Python
python -m http.server 8000

# Или используя Node.js (http-server)
npx http-server
```

2. Откройте браузер и перейдите на `http://localhost:8000`

3. Введите ваше имя и нажмите "Подключиться"

## 🎮 Возможности

### Сетевая игра
- ✅ Многопользовательский режим в реальном времени
- ✅ Синхронизация действий между игроками
- ✅ Система комнат/миров
- ✅ Видимость действий других игроков

### Игровые механики
- 🤖 Искусственные агенты (люди разных возрастов)
- 🐾 Домашние животные (коровы, козы, овцы, куры, кошки)
- 🐺 Хищники (волки, медведи, лисы)
- 🔥 Система температуры и костров
- 🏗️ Строительство и развитие
- 📦 Система инвентаря и ресурсов
- 🎓 Система опыта и навыков

### Взаимодействие
- Добавление ресурсов, животных, хищников
- Перемещение объектов мышкой
- Бесконечный мир с камерой
- Подсказки при наведении на объекты

## 📁 Структура проекта

```
artificial-life/
├── backend/              # Серверная часть
│   ├── server.js        # Основной сервер (Express + Socket.io)
│   └── package.json     # Зависимости
├── index.html           # Главная страница
├── main.js              # Основная логика клиента
├── world.js             # Логика мира и рендеринга
├── agents.js            # Логика агентов
├── network.js           # Сетевое взаимодействие
└── style.css            # Стили
```

## 🔧 Технологии

### Frontend
- Vanilla JavaScript
- HTML5 Canvas
- Socket.io Client

### Backend
- Node.js
- Express
- Socket.io
- CORS

## 🌐 Настройка для продакшена

1. Измените URL сервера в `network.js`:
```javascript
window.networkManager.connect('https://your-server.com');
```

2. Настройте CORS в `backend/server.js`:
```javascript
cors: {
    origin: "https://your-domain.com",
    methods: ["GET", "POST"]
}
```

3. Используйте переменные окружения для порта:
```bash
PORT=3000 npm start
```

## 📝 Разработка

### Добавление новых функций

1. **Новые ресурсы**: Добавьте кнопку в `index.html` и обработчик в `main.js`
2. **Новые действия**: Обновите `agents.js` и добавьте синхронизацию в `network.js`
3. **Новые события**: Добавьте обработчики в `backend/server.js` и `network.js`

### Отладка

- Откройте консоль браузера (F12) для просмотра логов
- Серверные логи выводятся в терминал
- Используйте вкладку "Логирование" в игре

## 🐛 Известные проблемы

- При большом количестве игроков может потребоваться оптимизация
- Рекомендуется использовать базу данных для персистентности миров

## 📄 Лицензия

ISC

## 👥 Авторы

Разработано для проекта Artificial Life
